# ═══════════════════════════════════════════════════════════════════════════
# ToolPool Backend — Python package list
# ═══════════════════════════════════════════════════════════════════════════
# WHY: pip installs every package listed here so your laptop and Docker
# get the SAME versions. That prevents "works on my machine" bugs.
#
# Install locally with:  pip install -r requirements.txt
# ═══════════════════════════════════════════════════════════════════════════

# Django = the web framework (handles URLs, models, admin, security)
Django==5.0.14

# Django REST Framework (DRF) = turns Django into a JSON API
djangorestframework==3.15.2

# SimpleJWT = creates/verifies JWT access + refresh tokens for login
djangorestframework-simplejwt==5.3.1

# django-cors-headers = lets the React frontend (different port/domain)
# call this API without the browser blocking the request
django-cors-headers==4.3.1

# django-filter = adds ?search=&category= query filters to list endpoints
django-filter==24.2

# psycopg2-binary = driver so Django can talk to PostgreSQL
# (binary = pre-compiled, easier to install than building from source)
psycopg2-binary==2.9.9

# Pillow = image library used by Django ImageField for tool photos
Pillow==10.4.0

# gunicorn = production web server (Docker/Render use this, not runserver)
gunicorn==22.0.0

# python-dotenv = loads SECRET_KEY etc. from a local .env file
python-dotenv==1.0.1

# whitenoise = serves Django static files in production without Nginx for them
whitenoise==6.7.0
